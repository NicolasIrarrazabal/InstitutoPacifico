spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

ms-matricula:
  url: ${MS_MATRICULA_URL}

logging:
  level:
    com.proyecto.ms_aranceles: INFO